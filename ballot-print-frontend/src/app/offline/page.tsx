// app/offline/page.tsx
export default function Offline() {
    return (
        <main style={{ padding: 24 }}>
            <h1>You’re offline</h1>
            <p>The app still works, but some features need a connection.</p>
        </main>
    );
}
